import {ConfirmProfile} from "./confirm-profile";

export class PageConfirmProfile {
  content: ConfirmProfile[] = [];
  last: boolean;
  totalElements: number;
  totalPages: number;
  size: number;
  number: number;
  first: boolean;
  numberOfElements: number;
}
